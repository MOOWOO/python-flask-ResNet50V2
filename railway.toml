[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "python3 ./Hair_loss/run_keras_server.py"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "never"